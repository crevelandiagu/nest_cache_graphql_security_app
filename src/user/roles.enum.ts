/* eslint-disable prettier/prettier */
export enum Role {
    ADMIN = "admin",
    READONLY = "read_only",
    RECETAREADONLY = "receta_read_only",
    WRTITEONLY = "write_only",
    DELETEONLY = "delete_only"
}